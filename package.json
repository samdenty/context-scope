{
  "name": "context-scope",
  "version": "1.0.2",
  "description": "Context (yes context) support for Javascript / Typescript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "https://github.com/samdenty99/context-scope",
  "author": "samdenty99",
  "license": "MIT",
  "scripts": {
    "start": "ts-node-dev --inspect=0.0.0.0:9000 --respawn src/demo.ts",
    "precommit": "pretty-quick --staged",
    "build": "rm -rf dist; tsc",
    "release": "yarn build && yarn publish"
  },
  "private": false,
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.13.5",
    "pretty-quick": "^1.6.0"
  },
  "dependencies": {
    "memoize-one": "^4.0.0"
  }
}
